parserImport XOCL;

import Docs;

context Docs

  @Class Block extends Doc

    @Attribute doc : Doc (?,!) end
    
    @Constructor(doc) ! end
    
    @Operation pprint(w,r,i,b,pt,pl,pr,s,f,cut)
      doc.pprint(w,r,pl,b,pt,pl,pr,s,f,cut)
    end

  end
