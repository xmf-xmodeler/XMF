parserImport XOCL;

import OCL;
import Clients;
import TextEditor;

context TextEditor

  @Class AttDescriptor extends VarDescriptor

    // A descriptor used by an EditorWalker to record a variable in the
    // environment when walking the body of a class...

  end