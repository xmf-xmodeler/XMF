parserImport XOCL;

import Clients;
import FmmlxDiagrams;

context FmmlxDiagrams

@Class MappingInfo

    //@Attribute object : Object end
    @Attribute id : String end
    @Attribute lastKnownDiagramX : Integer end // use the points list later (TODO)
    @Attribute lastKnownDiagramY : Integer end
    @Attribute points : Seq(Seq(Integer)) end // is indeed Vector(Vector(...)) for now
    @Attribute hidden : Boolean end

end // Class