parserImport XOCL;

import Clients;
import Diagrams;
import Menus;
import ClassDiagrams;

context ClassDiagrams

  @Class AssociationInstanceEdge extends Edge
  
    @Constructor(source,target) 
      self.init(Seq{null,source,target,Edge::noArrow,Edge::noArrow,Set{},Seq{},Color()});
      self.setStyle(Line::dottedLine)
    end
    
  end
  