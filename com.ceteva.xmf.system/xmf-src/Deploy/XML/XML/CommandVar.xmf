parserImport XOCL;
parserImport XML::PrintXML;

import XML::Parser;
import IO;
  
context CommandVar
  @Operation toXML(out:OutputChannel)
    @XML(out)
      <CommandVar name=name/>
    end
  end