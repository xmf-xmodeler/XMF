parserImport XOCL;
parserImport XML::PrintXML;

import XML::Parser;
import IO;
  
context CommandConst
  @Operation toXML(out:OutputChannel)
    @XML(out)
      <CommandConst value=value/>
    end
  end