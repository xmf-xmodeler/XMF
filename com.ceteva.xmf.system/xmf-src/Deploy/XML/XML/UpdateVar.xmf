parserImport XOCL;
parserImport XML::PrintXML;

import XML::Parser;
import IO;
  
context UpdateVar
  @Operation toXML(out:OutputChannel)
    @XML(out)
      <UpdateVar name=name/>
    end
  end