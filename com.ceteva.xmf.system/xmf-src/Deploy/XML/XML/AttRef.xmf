parserImport XOCL;
parserImport XML::PrintXML;

import XML::Parser;
import IO; 
  
context AttRef
  @Operation toXML(out:OutputChannel)
    @XML(out)
      <AttRef name=name/>
    end
  end 