parserImport XOCL;
parserImport XML::PrintXML;

import XML::Parser;
import IO;
  
context Strip
  @Operation toXML(out:OutputChannel)
    @XML(out)
      <Strip tag=tag isLast=isLast/>
    end
  end