parserImport XOCL;
parserImport Snippets;

@Snippet "Generating HTML Documentation to File System.";

// Call .generateHTML() on something you are interested in:

// XCore.generateHTML();
// Root.generateHTML();
Integer.generateHTML();

// Then specify folder for the HTML documentation to be generated into 
// when the next dialog asks for it. 